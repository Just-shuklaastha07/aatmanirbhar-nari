# Backend - Aatmanirbhar Nari

## Setup

1. Copy `.env.example` to `.env` and fill in your environment variables.
2. Run `npm install` to install dependencies.
3. Start the server with `npm run dev` (requires nodemon) or `node server.js`

## Scripts

- `npm run dev`: starts server with automatic reload on code change.
